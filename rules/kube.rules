groups:
- name: kube-rules
  interval: 30s # defaults to global interval
  rules:
    - alert: VersionCheck
      expr: openshift_build_info{job="kubernetes-apiservers"} == 1
      labels:
        severity: page
      annotations:
        summary: Wrong version

