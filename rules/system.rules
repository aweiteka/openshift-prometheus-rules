groups:
- name: system-rules
  interval: 30s # defaults to global interval
  rules:
    - alert: CPUCoreCheck
      expr: machine_cpu_cores != 4
      labels:
        severity: info
      annotations:
        summary: Incorrect CPU core

